// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package podsapikeys

import (
	"agentmail-cli/internal/client"
	"agentmail-cli/internal/flagutil"
	"agentmail-cli/internal/interactive"
	"agentmail-cli/internal/output"
	"agentmail-cli/internal/sdk/models/operations"
	"agentmail-cli/internal/usage"
	"fmt"
	"github.com/spf13/cobra"
	"strconv"
)

var podsAPIKeysCreateCmdMeta = []flagutil.FlagMeta{
	{FlagName: "pod-id", FieldPath: "PodID", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "name", Shorthand: "n", FieldPath: "Body.Name", Kind: flagutil.FlagKindString, Optional: true, Description: "Name of api key."},
	{FlagName: "permissions", FieldPath: "Body.Permissions", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"permissions,omitempty"`, Description: "Granular permissions for the API key. When ommitted all permissions are granted. Otherwise, only permissions set to true are granted."},
}

// initPodsApiKeysCreateCmd initializes the pods-api-keys-create command.
func initPodsApiKeysCreateCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "create",
		Short:   "Create API Key",
		Long:    "**CLI:**\n```bash\nagentmail pods:api-keys create --pod-id <pod_id> --name \"My Key\"\n```",
		Example: "  agentmail pods-api-keys create --pod-id <id>",
		RunE:    runPodsApiKeysCreateCmd,
	}
	flagutil.RegisterFlags(cmd, podsAPIKeysCreateCmdMeta)
	if err := flagutil.ValidateMeta[operations.PodsAPIKeysCreateRequest](podsAPIKeysCreateCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for pods-api-keys-create: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runPodsApiKeysCreateCmd executes the pods-api-keys-create command.
func runPodsApiKeysCreateCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, podsAPIKeysCreateCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, podsAPIKeysCreateCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.PodsAPIKeysCreateRequest](cmd, podsAPIKeysCreateCmdMeta, "Body", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if serverURL, _ := flagutil.GetStringFlag(cmd, "server-url"); serverURL != "" {
		sdkOpts = append(sdkOpts, operations.WithServerURL(serverURL))
	} else if serverFlag, _ := flagutil.GetStringFlag(cmd, "server"); serverFlag != "" {
		serverIdx, err := strconv.Atoi(serverFlag)
		if err != nil {
			return fmt.Errorf("invalid server index %q: must be an integer", serverFlag)
		}
		if serverIdx < 0 || serverIdx >= len(operations.PodsAPIKeysCreateServerList) {
			return fmt.Errorf("server index %d out of range (0-%d)", serverIdx, len(operations.PodsAPIKeysCreateServerList)-1)
		}
		serverURL := operations.PodsAPIKeysCreateServerList[serverIdx]
		sdkOpts = append(sdkOpts, operations.WithServerURL(serverURL))
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Pods.APIKeys.PodsAPIKeysCreate(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
