// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package podsthreads

import (
	"agentmail-cli/internal/client"
	"agentmail-cli/internal/flagutil"
	"agentmail-cli/internal/interactive"
	"agentmail-cli/internal/output"
	"agentmail-cli/internal/sdk/models/operations"
	"agentmail-cli/internal/usage"
	"fmt"
	"github.com/spf13/cobra"
	"strconv"
)

var podsThreadsDeleteCmdMeta = []flagutil.FlagMeta{
	{FlagName: "pod-id", FieldPath: "PodID", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "thread-id", Shorthand: "t", FieldPath: "ThreadID", Kind: flagutil.FlagKindString, Required: true, Description: "[required]"},
	{FlagName: "permanent", FieldPath: "Permanent", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `queryParam:"style=form,explode=true,name=permanent"`, Description: "If true, permanently delete the thread instead of moving to trash."},
}

// initPodsThreadsDeleteCmd initializes the pods-threads-delete command.
func initPodsThreadsDeleteCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "delete",
		Short:   "Delete Thread",
		Long:    "Moves the thread to trash by adding a trash label to all messages. If the thread is already in trash, it will be permanently deleted. Use `permanent=true` to force permanent deletion.\n\n**CLI:**\n```bash\nagentmail pods:threads delete --pod-id <pod_id> --thread-id <thread_id>\n```",
		Example: "  cli pods-threads delete --pod-id <id> --thread-id <id>",
		RunE:    runPodsThreadsDeleteCmd,
	}
	flagutil.RegisterFlags(cmd, podsThreadsDeleteCmdMeta)
	if err := flagutil.ValidateMeta[operations.PodsThreadsDeleteRequest](podsThreadsDeleteCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for pods-threads-delete: %w", err)
	}
	parent.AddCommand(cmd)
	return nil
}

// runPodsThreadsDeleteCmd executes the pods-threads-delete command.
func runPodsThreadsDeleteCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, podsThreadsDeleteCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, podsThreadsDeleteCmdMeta); err != nil {
			return err
		}
	}
	req, err := flagutil.BuildRequest[operations.PodsThreadsDeleteRequest](cmd, podsThreadsDeleteCmdMeta, "", "")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if serverURL, _ := flagutil.GetStringFlag(cmd, "server-url"); serverURL != "" {
		sdkOpts = append(sdkOpts, operations.WithServerURL(serverURL))
	} else if serverFlag, _ := flagutil.GetStringFlag(cmd, "server"); serverFlag != "" {
		serverIdx, err := strconv.Atoi(serverFlag)
		if err != nil {
			return fmt.Errorf("invalid server index %q: must be an integer", serverFlag)
		}
		if serverIdx < 0 || serverIdx >= len(operations.PodsThreadsDeleteServerList) {
			return fmt.Errorf("server index %d out of range (0-%d)", serverIdx, len(operations.PodsThreadsDeleteServerList)-1)
		}
		serverURL := operations.PodsThreadsDeleteServerList[serverIdx]
		sdkOpts = append(sdkOpts, operations.WithServerURL(serverURL))
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.PodsThreads.PodsThreadsDelete(cmd.Context(), *req, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
