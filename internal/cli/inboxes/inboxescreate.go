// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package inboxes

import (
	"agentmail-cli/internal/client"
	"agentmail-cli/internal/flagutil"
	"agentmail-cli/internal/interactive"
	"agentmail-cli/internal/output"
	"agentmail-cli/internal/sdk/models/components"
	"agentmail-cli/internal/sdk/models/operations"
	"agentmail-cli/internal/usage"
	"fmt"
	"github.com/spf13/cobra"
	"strconv"
)

var inboxesCreateCmdMeta = []flagutil.FlagMeta{
	{FlagName: "username", Shorthand: "u", FieldPath: "Username", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"username,omitempty"`, Description: "Username of address. Randomly generated if not specified."},
	{FlagName: "domain", FieldPath: "Domain", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"domain,omitempty"`, Description: "Domain of address. Must be verified domain. Defaults to `agentmail.to`."},
	{FlagName: "display-name", FieldPath: "DisplayName", Kind: flagutil.FlagKindString, Optional: true, Description: "Display name: `Display Name <username@domain.com>`."},
	{FlagName: "client-id", Shorthand: "c", FieldPath: "ClientID", Kind: flagutil.FlagKindString, Optional: true, Description: "Client ID of inbox."},
}

// initInboxesCreateCmd initializes the inboxes-create command.
func initInboxesCreateCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "create",
		Short:   "Create Inbox",
		Long:    "**CLI:**\n```bash\nagentmail inboxes create --display-name \"My Agent\" --username myagent --domain agentmail.to\n```",
		Example: "  agentmail inboxes create",
		RunE:    runInboxesCreateCmd,
	}
	flagutil.RegisterFlags(cmd, inboxesCreateCmdMeta)
	if err := flagutil.ValidateMeta[components.InboxesCreateInboxRequest](inboxesCreateCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for inboxes-create: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runInboxesCreateCmd executes the inboxes-create command.
func runInboxesCreateCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, inboxesCreateCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, inboxesCreateCmdMeta); err != nil {
			return err
		}
	}
	request, err := flagutil.BuildRequest[components.InboxesCreateInboxRequest](cmd, inboxesCreateCmdMeta, "", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if serverURL, _ := flagutil.GetStringFlag(cmd, "server-url"); serverURL != "" {
		sdkOpts = append(sdkOpts, operations.WithServerURL(serverURL))
	} else if serverFlag, _ := flagutil.GetStringFlag(cmd, "server"); serverFlag != "" {
		serverIdx, err := strconv.Atoi(serverFlag)
		if err != nil {
			return fmt.Errorf("invalid server index %q: must be an integer", serverFlag)
		}
		if serverIdx < 0 || serverIdx >= len(operations.InboxesCreateServerList) {
			return fmt.Errorf("server index %d out of range (0-%d)", serverIdx, len(operations.InboxesCreateServerList)-1)
		}
		serverURL := operations.InboxesCreateServerList[serverIdx]
		sdkOpts = append(sdkOpts, operations.WithServerURL(serverURL))
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.Inboxes.InboxesCreate(cmd.Context(), request, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
