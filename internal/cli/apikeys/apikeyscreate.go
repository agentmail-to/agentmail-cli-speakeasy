// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package apikeys

import (
	"agentmail-cli/internal/client"
	"agentmail-cli/internal/flagutil"
	"agentmail-cli/internal/interactive"
	"agentmail-cli/internal/output"
	"agentmail-cli/internal/sdk/models/components"
	"agentmail-cli/internal/sdk/models/operations"
	"agentmail-cli/internal/usage"
	"fmt"
	"github.com/spf13/cobra"
	"strconv"
)

var apiKeysCreateCmdMeta = []flagutil.FlagMeta{
	{FlagName: "name", Shorthand: "n", FieldPath: "Name", Kind: flagutil.FlagKindString, Optional: true, Description: "Name of api key."},
	{FlagName: "permissions", Shorthand: "p", FieldPath: "Permissions", Kind: flagutil.FlagKindJSON, Optional: true, Annotations: `json:"permissions,omitempty"`, Description: "Granular permissions for the API key. When ommitted all permissions are granted. Otherwise, only permissions set to true are granted."},
}

// initApiKeysCreateCmd initializes the api-keys-create command.
func initApiKeysCreateCmd(parent *cobra.Command) error {
	var cmd = &cobra.Command{
		Use:     "create",
		Short:   "Create API Key",
		Long:    "**CLI:**\n```bash\nagentmail api-keys create --name \"My Key\"\n```",
		Example: "  agentmail api-keys create",
		RunE:    runApiKeysCreateCmd,
	}
	flagutil.RegisterFlags(cmd, apiKeysCreateCmdMeta)
	if err := flagutil.ValidateMeta[components.CreateAPIKeyRequest](apiKeysCreateCmdMeta); err != nil {
		return fmt.Errorf("invalid metadata for api-keys-create: %w", err)
	}
	cmd.Flags().String("body", "", "Request body as JSON (alternative to individual flags). Can also be provided via stdin.")
	parent.AddCommand(cmd)
	return nil
}

// runApiKeysCreateCmd executes the api-keys-create command.
func runApiKeysCreateCmd(cmd *cobra.Command, args []string) error {
	if usage.UsageRequested(cmd) {
		return usage.EmitSchema(cmd, cmd.OutOrStdout())
	}
	if interactive.ShouldPrompt(cmd, apiKeysCreateCmdMeta) {
		if err := interactive.PromptAndSetFlags(cmd, apiKeysCreateCmdMeta); err != nil {
			return err
		}
	}
	request, err := flagutil.BuildRequest[components.CreateAPIKeyRequest](cmd, apiKeysCreateCmdMeta, "", "body")
	if err != nil {
		return err
	}
	s, err := client.NewClient(cmd)
	if err != nil {
		return err
	}
	sdkOpts, err := output.PrepareCallOpts(cmd)
	if err != nil {
		return err
	}
	// Dry-run: force skip deserialization so the synthetic empty response
	// does not cause parse failures in typed response handling.
	if client.IsDryRun(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	if serverURL, _ := flagutil.GetStringFlag(cmd, "server-url"); serverURL != "" {
		sdkOpts = append(sdkOpts, operations.WithServerURL(serverURL))
	} else if serverFlag, _ := flagutil.GetStringFlag(cmd, "server"); serverFlag != "" {
		serverIdx, err := strconv.Atoi(serverFlag)
		if err != nil {
			return fmt.Errorf("invalid server index %q: must be an integer", serverFlag)
		}
		if serverIdx < 0 || serverIdx >= len(operations.APIKeysCreateServerList) {
			return fmt.Errorf("server index %d out of range (0-%d)", serverIdx, len(operations.APIKeysCreateServerList)-1)
		}
		serverURL := operations.APIKeysCreateServerList[serverIdx]
		sdkOpts = append(sdkOpts, operations.WithServerURL(serverURL))
	}
	if output.WantsRawJSON(cmd) {
		sdkOpts = append(sdkOpts, operations.WithSkipDeserialization())
	}
	res, err := s.APIKeys.APIKeysCreate(cmd.Context(), *request, sdkOpts...)
	if err != nil {
		return output.Error(cmd, err)
	}

	if err := output.Result(cmd, res); err != nil {
		return err
	}
	return nil
}
