"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from agentmail.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class WebhooksUpdateWebhookRequestTypedDict(TypedDict):
    add_inbox_ids: NotRequired[List[str]]
    r"""Inboxes for which to send events. Maximum 10 per webhook."""
    remove_inbox_ids: NotRequired[List[str]]
    r"""Inboxes for which to send events. Maximum 10 per webhook."""
    add_pod_ids: NotRequired[List[str]]
    r"""Pods for which to send events. Maximum 10 per webhook."""
    remove_pod_ids: NotRequired[List[str]]
    r"""Pods for which to send events. Maximum 10 per webhook."""


class WebhooksUpdateWebhookRequest(BaseModel):
    add_inbox_ids: Optional[List[str]] = None
    r"""Inboxes for which to send events. Maximum 10 per webhook."""

    remove_inbox_ids: Optional[List[str]] = None
    r"""Inboxes for which to send events. Maximum 10 per webhook."""

    add_pod_ids: Optional[List[str]] = None
    r"""Pods for which to send events. Maximum 10 per webhook."""

    remove_pod_ids: Optional[List[str]] = None
    r"""Pods for which to send events. Maximum 10 per webhook."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["add_inbox_ids", "remove_inbox_ids", "add_pod_ids", "remove_pod_ids"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
