/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inboxesDraftsInboxesDraftsCreate } from "../funcs/inboxes-drafts-inboxes-drafts-create.js";
import { inboxesDraftsInboxesDraftsDelete } from "../funcs/inboxes-drafts-inboxes-drafts-delete.js";
import { inboxesDraftsInboxesDraftsGetAttachment } from "../funcs/inboxes-drafts-inboxes-drafts-get-attachment.js";
import { inboxesDraftsInboxesDraftsGet } from "../funcs/inboxes-drafts-inboxes-drafts-get.js";
import { inboxesDraftsInboxesDraftsList } from "../funcs/inboxes-drafts-inboxes-drafts-list.js";
import { inboxesDraftsInboxesDraftsSend } from "../funcs/inboxes-drafts-inboxes-drafts-send.js";
import { inboxesDraftsInboxesDraftsUpdate } from "../funcs/inboxes-drafts-inboxes-drafts-update.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class InboxesDrafts extends ClientSDK {
  /**
   * List Drafts
   *
   * @remarks
   * **CLI:**
   * ```bash
   * agentmail inboxes:drafts list --inbox-id <inbox_id>
   * ```
   */
  async inboxesDraftsList(
    request: operations.InboxesDraftsListRequest,
    options?: RequestOptions,
  ): Promise<components.ListDraftsResponse> {
    return unwrapAsync(inboxesDraftsInboxesDraftsList(
      this,
      request,
      options,
    ));
  }

  /**
   * Create Draft
   *
   * @remarks
   * **CLI:**
   * ```bash
   * agentmail inboxes:drafts create --inbox-id <inbox_id> --to recipient@example.com --subject "Draft subject" --text "Draft body"
   * ```
   */
  async inboxesDraftsCreate(
    inboxId: string,
    body: components.CreateDraftRequest,
    options?: RequestOptions,
  ): Promise<components.Draft> {
    return unwrapAsync(inboxesDraftsInboxesDraftsCreate(
      this,
      inboxId,
      body,
      options,
    ));
  }

  /**
   * Get Draft
   *
   * @remarks
   * **CLI:**
   * ```bash
   * agentmail inboxes:drafts get --inbox-id <inbox_id> --draft-id <draft_id>
   * ```
   */
  async inboxesDraftsGet(
    inboxId: string,
    draftId: string,
    options?: RequestOptions,
  ): Promise<components.Draft> {
    return unwrapAsync(inboxesDraftsInboxesDraftsGet(
      this,
      inboxId,
      draftId,
      options,
    ));
  }

  /**
   * Update Draft
   *
   * @remarks
   * **CLI:**
   * ```bash
   * agentmail inboxes:drafts update --inbox-id <inbox_id> --draft-id <draft_id> --subject "Updated subject"
   * ```
   */
  async inboxesDraftsUpdate(
    inboxId: string,
    draftId: string,
    body: components.UpdateDraftRequest,
    options?: RequestOptions,
  ): Promise<components.Draft> {
    return unwrapAsync(inboxesDraftsInboxesDraftsUpdate(
      this,
      inboxId,
      draftId,
      body,
      options,
    ));
  }

  /**
   * Delete Draft
   *
   * @remarks
   * **CLI:**
   * ```bash
   * agentmail inboxes:drafts delete --inbox-id <inbox_id> --draft-id <draft_id>
   * ```
   */
  async inboxesDraftsDelete(
    inboxId: string,
    draftId: string,
    options?: RequestOptions,
  ): Promise<void> {
    return unwrapAsync(inboxesDraftsInboxesDraftsDelete(
      this,
      inboxId,
      draftId,
      options,
    ));
  }

  /**
   * Get Attachment
   *
   * @remarks
   * **CLI:**
   * ```bash
   * agentmail inboxes:drafts get-attachment --inbox-id <inbox_id> --draft-id <draft_id> --attachment-id <attachment_id>
   * ```
   */
  async inboxesDraftsGetAttachment(
    inboxId: string,
    draftId: string,
    attachmentId: string,
    options?: RequestOptions,
  ): Promise<components.AttachmentResponse> {
    return unwrapAsync(inboxesDraftsInboxesDraftsGetAttachment(
      this,
      inboxId,
      draftId,
      attachmentId,
      options,
    ));
  }

  /**
   * Send Draft
   *
   * @remarks
   * **CLI:**
   * ```bash
   * agentmail inboxes:drafts send --inbox-id <inbox_id> --draft-id <draft_id>
   * ```
   */
  async inboxesDraftsSend(
    inboxId: string,
    draftId: string,
    body: components.UpdateMessageRequest,
    options?: RequestOptions,
  ): Promise<components.SendMessageResponse> {
    return unwrapAsync(inboxesDraftsInboxesDraftsSend(
      this,
      inboxId,
      draftId,
      body,
      options,
    ));
  }
}
