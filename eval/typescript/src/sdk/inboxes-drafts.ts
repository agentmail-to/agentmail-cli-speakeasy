/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inboxesDraftsCreate } from "../funcs/inboxes-drafts-create.js";
import { inboxesDraftsDelete } from "../funcs/inboxes-drafts-delete.js";
import { inboxesDraftsGetAttachment } from "../funcs/inboxes-drafts-get-attachment.js";
import { inboxesDraftsGet } from "../funcs/inboxes-drafts-get.js";
import { inboxesDraftsList } from "../funcs/inboxes-drafts-list.js";
import { inboxesDraftsSend } from "../funcs/inboxes-drafts-send.js";
import { inboxesDraftsUpdate } from "../funcs/inboxes-drafts-update.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class InboxesDrafts extends ClientSDK {
  /**
   * List Drafts
   *
   * @remarks
   * **CLI:**
   * ```bash
   * agentmail inboxes:drafts list --inbox-id <inbox_id>
   * ```
   */
  async list(
    request: operations.InboxesDraftsListRequest,
    options?: RequestOptions,
  ): Promise<components.ListDraftsResponse> {
    return unwrapAsync(inboxesDraftsList(
      this,
      request,
      options,
    ));
  }

  /**
   * Create Draft
   *
   * @remarks
   * **CLI:**
   * ```bash
   * agentmail inboxes:drafts create --inbox-id <inbox_id> --to recipient@example.com --subject "Draft subject" --text "Draft body"
   * ```
   */
  async create(
    inboxId: string,
    body: components.CreateDraftRequest,
    options?: RequestOptions,
  ): Promise<components.Draft> {
    return unwrapAsync(inboxesDraftsCreate(
      this,
      inboxId,
      body,
      options,
    ));
  }

  /**
   * Get Draft
   *
   * @remarks
   * **CLI:**
   * ```bash
   * agentmail inboxes:drafts get --inbox-id <inbox_id> --draft-id <draft_id>
   * ```
   */
  async get(
    inboxId: string,
    draftId: string,
    options?: RequestOptions,
  ): Promise<components.Draft> {
    return unwrapAsync(inboxesDraftsGet(
      this,
      inboxId,
      draftId,
      options,
    ));
  }

  /**
   * Update Draft
   *
   * @remarks
   * **CLI:**
   * ```bash
   * agentmail inboxes:drafts update --inbox-id <inbox_id> --draft-id <draft_id> --subject "Updated subject"
   * ```
   */
  async update(
    inboxId: string,
    draftId: string,
    body: components.UpdateDraftRequest,
    options?: RequestOptions,
  ): Promise<components.Draft> {
    return unwrapAsync(inboxesDraftsUpdate(
      this,
      inboxId,
      draftId,
      body,
      options,
    ));
  }

  /**
   * Delete Draft
   *
   * @remarks
   * **CLI:**
   * ```bash
   * agentmail inboxes:drafts delete --inbox-id <inbox_id> --draft-id <draft_id>
   * ```
   */
  async delete(
    inboxId: string,
    draftId: string,
    options?: RequestOptions,
  ): Promise<void> {
    return unwrapAsync(inboxesDraftsDelete(
      this,
      inboxId,
      draftId,
      options,
    ));
  }

  /**
   * Get Attachment
   *
   * @remarks
   * **CLI:**
   * ```bash
   * agentmail inboxes:drafts get-attachment --inbox-id <inbox_id> --draft-id <draft_id> --attachment-id <attachment_id>
   * ```
   */
  async getAttachment(
    inboxId: string,
    draftId: string,
    attachmentId: string,
    options?: RequestOptions,
  ): Promise<components.AttachmentResponse> {
    return unwrapAsync(inboxesDraftsGetAttachment(
      this,
      inboxId,
      draftId,
      attachmentId,
      options,
    ));
  }

  /**
   * Send Draft
   *
   * @remarks
   * **CLI:**
   * ```bash
   * agentmail inboxes:drafts send --inbox-id <inbox_id> --draft-id <draft_id>
   * ```
   */
  async send(
    inboxId: string,
    draftId: string,
    body: components.UpdateMessageRequest,
    options?: RequestOptions,
  ): Promise<components.SendMessageResponse> {
    return unwrapAsync(inboxesDraftsSend(
      this,
      inboxId,
      draftId,
      body,
      options,
    ));
  }
}
