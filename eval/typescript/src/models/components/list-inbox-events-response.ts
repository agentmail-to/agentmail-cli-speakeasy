/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import { InboxEvent, InboxEvent$inboundSchema } from "./inbox-event.js";

export type ListInboxEventsResponse = {
  /**
   * Number of items returned.
   */
  count: number;
  /**
   * Limit of number of items returned.
   */
  limit?: number | undefined;
  /**
   * Page token for pagination.
   */
  nextPageToken?: string | undefined;
  /**
   * Ordered by `event_id` descending.
   */
  events: Array<InboxEvent>;
};

/** @internal */
export const ListInboxEventsResponse$inboundSchema: z.ZodMiniType<
  ListInboxEventsResponse,
  unknown
> = z.pipe(
  z.object({
    count: types.number(),
    limit: types.optional(types.number()),
    next_page_token: types.optional(types.string()),
    events: z.array(InboxEvent$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "next_page_token": "nextPageToken",
    });
  }),
);

export function listInboxEventsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListInboxEventsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListInboxEventsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListInboxEventsResponse' from JSON`,
  );
}
