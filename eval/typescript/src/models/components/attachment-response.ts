/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  AttachmentContentDisposition,
  AttachmentContentDisposition$inboundSchema,
} from "./attachment-content-disposition.js";

export type AttachmentResponse = {
  /**
   * ID of attachment.
   */
  attachmentId: string;
  /**
   * Filename of attachment.
   */
  filename?: string | undefined;
  /**
   * Size of attachment in bytes.
   */
  size: number;
  /**
   * Content type of attachment.
   */
  contentType?: string | undefined;
  /**
   * Content disposition of attachment.
   */
  contentDisposition?: AttachmentContentDisposition | undefined;
  /**
   * Content ID of attachment.
   */
  contentId?: string | undefined;
  /**
   * URL to download the attachment.
   */
  downloadUrl: string;
  /**
   * Time at which the download URL expires.
   */
  expiresAt: Date;
};

/** @internal */
export const AttachmentResponse$inboundSchema: z.ZodMiniType<
  AttachmentResponse,
  unknown
> = z.pipe(
  z.object({
    attachment_id: types.string(),
    filename: types.optional(types.string()),
    size: types.number(),
    content_type: types.optional(types.string()),
    content_disposition: types.optional(
      AttachmentContentDisposition$inboundSchema,
    ),
    content_id: types.optional(types.string()),
    download_url: types.string(),
    expires_at: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "attachment_id": "attachmentId",
      "content_type": "contentType",
      "content_disposition": "contentDisposition",
      "content_id": "contentId",
      "download_url": "downloadUrl",
      "expires_at": "expiresAt",
    });
  }),
);

export function attachmentResponseFromJSON(
  jsonString: string,
): SafeParseResult<AttachmentResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AttachmentResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AttachmentResponse' from JSON`,
  );
}
