/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import { InboxesInbox, InboxesInbox$inboundSchema } from "./inboxes-inbox.js";

export type InboxesListInboxesResponse = {
  /**
   * Number of items returned.
   */
  count: number;
  /**
   * Limit of number of items returned.
   */
  limit?: number | undefined;
  /**
   * Page token for pagination.
   */
  nextPageToken?: string | undefined;
  /**
   * Ordered by `created_at` descending.
   */
  inboxes: Array<InboxesInbox>;
};

/** @internal */
export const InboxesListInboxesResponse$inboundSchema: z.ZodMiniType<
  InboxesListInboxesResponse,
  unknown
> = z.pipe(
  z.object({
    count: types.number(),
    limit: types.optional(types.number()),
    next_page_token: types.optional(types.string()),
    inboxes: z.array(InboxesInbox$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "next_page_token": "nextPageToken",
    });
  }),
);

export function inboxesListInboxesResponseFromJSON(
  jsonString: string,
): SafeParseResult<InboxesListInboxesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxesListInboxesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxesListInboxesResponse' from JSON`,
  );
}
