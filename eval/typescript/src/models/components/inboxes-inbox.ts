/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

export type InboxesInbox = {
  /**
   * ID of pod.
   */
  podId: string;
  /**
   * The ID of the inbox.
   */
  inboxId: string;
  /**
   * Email address of the inbox.
   */
  email: string;
  /**
   * Display name: `Display Name <username@domain.com>`.
   */
  displayName?: string | undefined;
  /**
   * Client ID of inbox.
   */
  clientId?: string | undefined;
  /**
   * Time at which inbox was last updated.
   */
  updatedAt: Date;
  /**
   * Time at which inbox was created.
   */
  createdAt: Date;
};

/** @internal */
export const InboxesInbox$inboundSchema: z.ZodMiniType<InboxesInbox, unknown> =
  z.pipe(
    z.object({
      pod_id: types.string(),
      inbox_id: types.string(),
      email: types.string(),
      display_name: types.optional(types.string()),
      client_id: types.optional(types.string()),
      updated_at: types.date(),
      created_at: types.date(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "pod_id": "podId",
        "inbox_id": "inboxId",
        "display_name": "displayName",
        "client_id": "clientId",
        "updated_at": "updatedAt",
        "created_at": "createdAt",
      });
    }),
  );

export function inboxesInboxFromJSON(
  jsonString: string,
): SafeParseResult<InboxesInbox, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxesInbox$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxesInbox' from JSON`,
  );
}
