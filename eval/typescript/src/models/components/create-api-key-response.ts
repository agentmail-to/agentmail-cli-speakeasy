/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  ApiKeyPermissions,
  ApiKeyPermissions$inboundSchema,
} from "./api-key-permissions.js";

export type CreateApiKeyResponse = {
  /**
   * ID of api key.
   */
  apiKeyId: string;
  /**
   * API key.
   */
  apiKey: string;
  /**
   * Prefix of api key.
   */
  prefix: string;
  /**
   * Name of api key.
   */
  name: string;
  /**
   * Pod ID the api key is scoped to.
   */
  podId?: string | null | undefined;
  /**
   * Inbox ID the api key is scoped to.
   */
  inboxId?: string | null | undefined;
  /**
   * Granular permissions for the API key. When ommitted all permissions are granted. Otherwise, only permissions set to true are granted.
   */
  permissions?: ApiKeyPermissions | undefined;
  /**
   * Time at which api key was created.
   */
  createdAt: Date;
};

/** @internal */
export const CreateApiKeyResponse$inboundSchema: z.ZodMiniType<
  CreateApiKeyResponse,
  unknown
> = z.pipe(
  z.object({
    api_key_id: types.string(),
    api_key: types.string(),
    prefix: types.string(),
    name: types.string(),
    pod_id: z.optional(z.nullable(types.string())),
    inbox_id: z.optional(z.nullable(types.string())),
    permissions: types.optional(ApiKeyPermissions$inboundSchema),
    created_at: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "api_key_id": "apiKeyId",
      "api_key": "apiKey",
      "pod_id": "podId",
      "inbox_id": "inboxId",
      "created_at": "createdAt",
    });
  }),
);

export function createApiKeyResponseFromJSON(
  jsonString: string,
): SafeParseResult<CreateApiKeyResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateApiKeyResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateApiKeyResponse' from JSON`,
  );
}
