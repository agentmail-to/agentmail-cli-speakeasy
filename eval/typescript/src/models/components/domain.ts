/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import { Status, Status$inboundSchema } from "./status.js";
import {
  VerificationRecord,
  VerificationRecord$inboundSchema,
} from "./verification-record.js";

export type Domain = {
  /**
   * ID of pod.
   */
  podId?: string | undefined;
  /**
   * The ID of the domain.
   */
  domainId: string;
  /**
   * The name of the domain (e.g., `example.com`).
   */
  domain: string;
  status: Status;
  /**
   * Bounce and complaint notifications are sent to your inboxes.
   */
  feedbackEnabled: boolean;
  /**
   * A list of DNS records required to verify the domain.
   */
  records: Array<VerificationRecord>;
  /**
   * Client ID of domain.
   */
  clientId?: string | undefined;
  /**
   * Time at which the domain was last updated.
   */
  updatedAt: Date;
  /**
   * Time at which the domain was created.
   */
  createdAt: Date;
};

/** @internal */
export const Domain$inboundSchema: z.ZodMiniType<Domain, unknown> = z.pipe(
  z.object({
    pod_id: types.optional(types.string()),
    domain_id: types.string(),
    domain: types.string(),
    status: Status$inboundSchema,
    feedback_enabled: types.boolean(),
    records: z.array(VerificationRecord$inboundSchema),
    client_id: types.optional(types.string()),
    updated_at: types.date(),
    created_at: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "pod_id": "podId",
      "domain_id": "domainId",
      "feedback_enabled": "feedbackEnabled",
      "client_id": "clientId",
      "updated_at": "updatedAt",
      "created_at": "createdAt",
    });
  }),
);

export function domainFromJSON(
  jsonString: string,
): SafeParseResult<Domain, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Domain$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Domain' from JSON`,
  );
}
