/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

export type DomainItem = {
  /**
   * ID of pod.
   */
  podId?: string | undefined;
  /**
   * The ID of the domain.
   */
  domainId: string;
  /**
   * The name of the domain (e.g., `example.com`).
   */
  domain: string;
  /**
   * Bounce and complaint notifications are sent to your inboxes.
   */
  feedbackEnabled: boolean;
  /**
   * Client ID of domain.
   */
  clientId?: string | undefined;
  /**
   * Time at which the domain was last updated.
   */
  updatedAt: Date;
  /**
   * Time at which the domain was created.
   */
  createdAt: Date;
};

/** @internal */
export const DomainItem$inboundSchema: z.ZodMiniType<DomainItem, unknown> = z
  .pipe(
    z.object({
      pod_id: types.optional(types.string()),
      domain_id: types.string(),
      domain: types.string(),
      feedback_enabled: types.boolean(),
      client_id: types.optional(types.string()),
      updated_at: types.date(),
      created_at: types.date(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "pod_id": "podId",
        "domain_id": "domainId",
        "feedback_enabled": "feedbackEnabled",
        "client_id": "clientId",
        "updated_at": "updatedAt",
        "created_at": "createdAt",
      });
    }),
  );

export function domainItemFromJSON(
  jsonString: string,
): SafeParseResult<DomainItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DomainItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DomainItem' from JSON`,
  );
}
