/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * S3 presigned URL to download the raw .eml file.
 */
export type RawMessageResponse = {
  /**
   * ID of message.
   */
  messageId: string;
  /**
   * Size of message in bytes.
   */
  size: number;
  /**
   * S3 presigned URL to download the raw message. Expires at expires_at.
   */
  downloadUrl: string;
  /**
   * Time at which the download URL expires.
   */
  expiresAt: Date;
};

/** @internal */
export const RawMessageResponse$inboundSchema: z.ZodMiniType<
  RawMessageResponse,
  unknown
> = z.pipe(
  z.object({
    message_id: types.string(),
    size: types.number(),
    download_url: types.string(),
    expires_at: types.date(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "message_id": "messageId",
      "download_url": "downloadUrl",
      "expires_at": "expiresAt",
    });
  }),
);

export function rawMessageResponseFromJSON(
  jsonString: string,
): SafeParseResult<RawMessageResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RawMessageResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RawMessageResponse' from JSON`,
  );
}
