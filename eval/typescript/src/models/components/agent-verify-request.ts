/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

/**
 * Request body to verify an agent with an OTP code.
 */
export type AgentVerifyRequest = {
  /**
   * 6-digit verification code sent to the human's email address.
   */
  otpCode: string;
};

/** @internal */
export type AgentVerifyRequest$Outbound = {
  otp_code: string;
};

/** @internal */
export const AgentVerifyRequest$outboundSchema: z.ZodMiniType<
  AgentVerifyRequest$Outbound,
  AgentVerifyRequest
> = z.pipe(
  z.object({
    otpCode: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      otpCode: "otp_code",
    });
  }),
);

export function agentVerifyRequestToJSON(
  agentVerifyRequest: AgentVerifyRequest,
): string {
  return JSON.stringify(
    AgentVerifyRequest$outboundSchema.parse(agentVerifyRequest),
  );
}
