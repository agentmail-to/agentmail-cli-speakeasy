/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

export type PodsPod = {
  /**
   * ID of pod.
   */
  podId: string;
  /**
   * Name of pod.
   */
  name: string;
  /**
   * Time at which pod was last updated.
   */
  updatedAt: Date;
  /**
   * Time at which pod was created.
   */
  createdAt: Date;
  /**
   * Client ID of pod.
   */
  clientId?: string | undefined;
};

/** @internal */
export const PodsPod$inboundSchema: z.ZodMiniType<PodsPod, unknown> = z.pipe(
  z.object({
    pod_id: types.string(),
    name: types.string(),
    updated_at: types.date(),
    created_at: types.date(),
    client_id: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "pod_id": "podId",
      "updated_at": "updatedAt",
      "created_at": "createdAt",
      "client_id": "clientId",
    });
  }),
);

export function podsPodFromJSON(
  jsonString: string,
): SafeParseResult<PodsPod, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PodsPod$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PodsPod' from JSON`,
  );
}
