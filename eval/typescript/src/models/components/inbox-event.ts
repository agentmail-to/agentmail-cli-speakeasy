/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import {
  InboxEventType,
  InboxEventType$inboundSchema,
} from "./inbox-event-type.js";

export type InboxEvent = {
  /**
   * ID of organization.
   */
  organizationId: string;
  /**
   * ID of pod.
   */
  podId: string;
  /**
   * The ID of the inbox.
   */
  inboxId: string;
  /**
   * ID of event.
   */
  eventId: string;
  /**
   * Type of inbox event.
   */
  eventType: InboxEventType;
  /**
   * ID of message.
   */
  messageId: string;
  /**
   * Label added or removed.
   */
  label: string;
  /**
   * Time at which the event occurred.
   */
  eventAt: Date;
  /**
   * Time at which the event was recorded.
   */
  createdAt: Date;
};

/** @internal */
export const InboxEvent$inboundSchema: z.ZodMiniType<InboxEvent, unknown> = z
  .pipe(
    z.object({
      organization_id: types.string(),
      pod_id: types.string(),
      inbox_id: types.string(),
      event_id: types.string(),
      event_type: InboxEventType$inboundSchema,
      message_id: types.string(),
      label: types.string(),
      event_at: types.date(),
      created_at: types.date(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "organization_id": "organizationId",
        "pod_id": "podId",
        "inbox_id": "inboxId",
        "event_id": "eventId",
        "event_type": "eventType",
        "message_id": "messageId",
        "event_at": "eventAt",
        "created_at": "createdAt",
      });
    }),
  );

export function inboxEventFromJSON(
  jsonString: string,
): SafeParseResult<InboxEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => InboxEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'InboxEvent' from JSON`,
  );
}
