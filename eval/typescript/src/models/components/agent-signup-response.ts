/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

/**
 * Response after successful agent sign-up.
 */
export type AgentSignupResponse = {
  /**
   * ID of the created organization.
   */
  organizationId: string;
  /**
   * ID of the auto-created inbox.
   */
  inboxId: string;
  /**
   * API key for authenticating subsequent requests. Store this securely, it cannot be retrieved again.
   */
  apiKey: string;
};

/** @internal */
export const AgentSignupResponse$inboundSchema: z.ZodMiniType<
  AgentSignupResponse,
  unknown
> = z.pipe(
  z.object({
    organization_id: types.string(),
    inbox_id: types.string(),
    api_key: types.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "organization_id": "organizationId",
      "inbox_id": "inboxId",
      "api_key": "apiKey",
    });
  }),
);

export function agentSignupResponseFromJSON(
  jsonString: string,
): SafeParseResult<AgentSignupResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentSignupResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentSignupResponse' from JSON`,
  );
}
