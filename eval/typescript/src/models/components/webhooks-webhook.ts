/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import { EventType, EventType$inboundSchema } from "./event-type.js";

export type WebhooksWebhook = {
  /**
   * ID of webhook.
   */
  webhookId: string;
  /**
   * URL of webhook endpoint.
   */
  url: string;
  /**
   * Event types for which to send events.
   */
  eventTypes?: Array<EventType> | undefined;
  /**
   * Pods for which to send events. Maximum 10 per webhook.
   */
  podIds?: Array<string> | undefined;
  /**
   * Inboxes for which to send events. Maximum 10 per webhook.
   */
  inboxIds?: Array<string> | undefined;
  /**
   * Secret for webhook signature verification.
   */
  secret: string;
  /**
   * Webhook is enabled.
   */
  enabled: boolean;
  /**
   * Time at which webhook was last updated.
   */
  updatedAt: Date;
  /**
   * Time at which webhook was created.
   */
  createdAt: Date;
  /**
   * Client ID of webhook.
   */
  clientId?: string | undefined;
};

/** @internal */
export const WebhooksWebhook$inboundSchema: z.ZodMiniType<
  WebhooksWebhook,
  unknown
> = z.pipe(
  z.object({
    webhook_id: types.string(),
    url: types.string(),
    event_types: types.optional(z.array(EventType$inboundSchema)),
    pod_ids: types.optional(z.array(types.string())),
    inbox_ids: types.optional(z.array(types.string())),
    secret: types.string(),
    enabled: types.boolean(),
    updated_at: types.date(),
    created_at: types.date(),
    client_id: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "webhook_id": "webhookId",
      "event_types": "eventTypes",
      "pod_ids": "podIds",
      "inbox_ids": "inboxIds",
      "updated_at": "updatedAt",
      "created_at": "createdAt",
      "client_id": "clientId",
    });
  }),
);

export function webhooksWebhookFromJSON(
  jsonString: string,
): SafeParseResult<WebhooksWebhook, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WebhooksWebhook$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WebhooksWebhook' from JSON`,
  );
}
