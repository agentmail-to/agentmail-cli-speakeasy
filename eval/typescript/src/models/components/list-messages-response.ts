/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import { MessageItem, MessageItem$inboundSchema } from "./message-item.js";

export type ListMessagesResponse = {
  /**
   * Number of items returned.
   */
  count: number;
  /**
   * Limit of number of items returned.
   */
  limit?: number | undefined;
  /**
   * Page token for pagination.
   */
  nextPageToken?: string | undefined;
  /**
   * Ordered by `timestamp` descending.
   */
  messages: Array<MessageItem>;
};

/** @internal */
export const ListMessagesResponse$inboundSchema: z.ZodMiniType<
  ListMessagesResponse,
  unknown
> = z.pipe(
  z.object({
    count: types.number(),
    limit: types.optional(types.number()),
    next_page_token: types.optional(types.string()),
    messages: z.array(MessageItem$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "next_page_token": "nextPageToken",
    });
  }),
);

export function listMessagesResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListMessagesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListMessagesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListMessagesResponse' from JSON`,
  );
}
