/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

/**
 * Request body to sign up an agent.
 */
export type AgentSignupRequest = {
  /**
   * Email address of the human who owns the agent. A 6-digit OTP will be sent to this address.
   */
  humanEmail: string;
  /**
   * Username for the auto-created inbox (e.g. "my-agent" creates my-agent@agentmail.to).
   */
  username: string;
};

/** @internal */
export type AgentSignupRequest$Outbound = {
  human_email: string;
  username: string;
};

/** @internal */
export const AgentSignupRequest$outboundSchema: z.ZodMiniType<
  AgentSignupRequest$Outbound,
  AgentSignupRequest
> = z.pipe(
  z.object({
    humanEmail: z.string(),
    username: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      humanEmail: "human_email",
    });
  }),
);

export function agentSignupRequestToJSON(
  agentSignupRequest: AgentSignupRequest,
): string {
  return JSON.stringify(
    AgentSignupRequest$outboundSchema.parse(agentSignupRequest),
  );
}
