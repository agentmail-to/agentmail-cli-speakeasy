/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";

export type UpdateThreadResponse = {
  /**
   * ID of thread.
   */
  threadId: string;
  /**
   * Labels of thread.
   */
  labels: Array<string>;
};

/** @internal */
export const UpdateThreadResponse$inboundSchema: z.ZodMiniType<
  UpdateThreadResponse,
  unknown
> = z.pipe(
  z.object({
    thread_id: types.string(),
    labels: z.array(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "thread_id": "threadId",
    });
  }),
);

export function updateThreadResponseFromJSON(
  jsonString: string,
): SafeParseResult<UpdateThreadResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => UpdateThreadResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'UpdateThreadResponse' from JSON`,
  );
}
