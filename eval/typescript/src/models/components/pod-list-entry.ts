/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import { Direction, Direction$inboundSchema } from "./direction.js";
import { EntryType, EntryType$inboundSchema } from "./entry-type.js";
import { ListType, ListType$inboundSchema } from "./list-type.js";

export type PodListEntry = {
  /**
   * Email address or domain of list entry.
   */
  entry: string;
  /**
   * ID of organization.
   */
  organizationId: string;
  /**
   * Reason for adding the entry.
   */
  reason?: string | null | undefined;
  /**
   * Direction of list entry.
   */
  direction: Direction;
  /**
   * Type of list entry.
   */
  listType: ListType;
  /**
   * Whether the entry is an email address or domain.
   */
  entryType: EntryType;
  /**
   * Time at which entry was created.
   */
  createdAt: Date;
  /**
   * ID of pod.
   */
  podId: string;
  /**
   * ID of inbox, if entry is inbox-scoped.
   */
  inboxId?: string | null | undefined;
};

/** @internal */
export const PodListEntry$inboundSchema: z.ZodMiniType<PodListEntry, unknown> =
  z.pipe(
    z.object({
      entry: types.string(),
      organization_id: types.string(),
      reason: z.optional(z.nullable(types.string())),
      direction: Direction$inboundSchema,
      list_type: ListType$inboundSchema,
      entry_type: EntryType$inboundSchema,
      created_at: types.date(),
      pod_id: types.string(),
      inbox_id: z.optional(z.nullable(types.string())),
    }),
    z.transform((v) => {
      return remap$(v, {
        "organization_id": "organizationId",
        "list_type": "listType",
        "entry_type": "entryType",
        "created_at": "createdAt",
        "pod_id": "podId",
        "inbox_id": "inboxId",
      });
    }),
  );

export function podListEntryFromJSON(
  jsonString: string,
): SafeParseResult<PodListEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PodListEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PodListEntry' from JSON`,
  );
}
