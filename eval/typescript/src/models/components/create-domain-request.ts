/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";

export type CreateDomainRequest = {
  /**
   * The name of the domain (e.g., `example.com`).
   */
  domain: string;
  /**
   * Bounce and complaint notifications are sent to your inboxes.
   */
  feedbackEnabled: boolean;
};

/** @internal */
export type CreateDomainRequest$Outbound = {
  domain: string;
  feedback_enabled: boolean;
};

/** @internal */
export const CreateDomainRequest$outboundSchema: z.ZodMiniType<
  CreateDomainRequest$Outbound,
  CreateDomainRequest
> = z.pipe(
  z.object({
    domain: z.string(),
    feedbackEnabled: z.boolean(),
  }),
  z.transform((v) => {
    return remap$(v, {
      feedbackEnabled: "feedback_enabled",
    });
  }),
);

export function createDomainRequestToJSON(
  createDomainRequest: CreateDomainRequest,
): string {
  return JSON.stringify(
    CreateDomainRequest$outboundSchema.parse(createDomainRequest),
  );
}
