/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdk-validation-error.js";
import { DraftItem, DraftItem$inboundSchema } from "./draft-item.js";

export type ListDraftsResponse = {
  /**
   * Number of items returned.
   */
  count: number;
  /**
   * Limit of number of items returned.
   */
  limit?: number | undefined;
  /**
   * Page token for pagination.
   */
  nextPageToken?: string | undefined;
  /**
   * Ordered by `updated_at` descending.
   */
  drafts: Array<DraftItem>;
};

/** @internal */
export const ListDraftsResponse$inboundSchema: z.ZodMiniType<
  ListDraftsResponse,
  unknown
> = z.pipe(
  z.object({
    count: types.number(),
    limit: types.optional(types.number()),
    next_page_token: types.optional(types.string()),
    drafts: z.array(DraftItem$inboundSchema),
  }),
  z.transform((v) => {
    return remap$(v, {
      "next_page_token": "nextPageToken",
    });
  }),
);

export function listDraftsResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListDraftsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListDraftsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListDraftsResponse' from JSON`,
  );
}
