/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { AgentmailCliCore } from "../core.js";
import { encodeJSON, encodeSimple } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import * as components from "../models/components/index.js";
import { AgentmailCliError } from "../models/errors/agentmail-cli-error.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/http-client-errors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/response-validation-error.js";
import { SDKValidationError } from "../models/errors/sdk-validation-error.js";
import * as operations from "../models/operations/index.js";
import { PodsApiKeysCreateServerList } from "../models/operations/pods-api-keys-create.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * Create API Key
 *
 * @remarks
 * **CLI:**
 * ```bash
 * agentmail pods:api-keys create --pod-id <pod_id> --name "My Key"
 * ```
 */
export function podsApiKeysPodsApiKeysCreate(
  client: AgentmailCliCore,
  podId: string,
  body: components.CreateApiKeyRequest,
  options?: RequestOptions,
): APIPromise<
  Result<
    components.CreateApiKeyResponse,
    | errors.ValidationErrorResponse
    | errors.ErrorResponse
    | AgentmailCliError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    podId,
    body,
    options,
  ));
}

async function $do(
  client: AgentmailCliCore,
  podId: string,
  body: components.CreateApiKeyRequest,
  options?: RequestOptions,
): Promise<
  [
    Result<
      components.CreateApiKeyResponse,
      | errors.ValidationErrorResponse
      | errors.ErrorResponse
      | AgentmailCliError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const input: operations.PodsApiKeysCreateRequest = {
    podId: podId,
    body: body,
  };

  const parsed = safeParse(
    input,
    (value) =>
      z.parse(operations.PodsApiKeysCreateRequest$outboundSchema, value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body$ = encodeJSON("body", payload.body, { explode: true });

  const baseURL = options?.serverURL
    || pathToFunc(PodsApiKeysCreateServerList[0], {
      charEncoding: "percent",
    })();

  const pathParams = {
    pod_id: encodeSimple("pod_id", payload.pod_id, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path = pathToFunc("/v0/pods/{pod_id}/api-keys")(pathParams);

  const headers = new Headers(compactMap({
    "Content-Type": "application/json",
    Accept: "application/json",
  }));

  const secConfig = await extractSecurity(client._options.bearerAuth);
  const securityInput = secConfig == null ? {} : { bearerAuth: secConfig };
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: baseURL ?? "",
    operationID: "pods_apiKeys_create",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.bearerAuth,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "POST",
    baseURL: baseURL,
    path: path,
    headers: headers,
    body: body$,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    components.CreateApiKeyResponse,
    | errors.ValidationErrorResponse
    | errors.ErrorResponse
    | AgentmailCliError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, components.CreateApiKeyResponse$inboundSchema),
    M.jsonErr(400, errors.ValidationErrorResponse$inboundSchema),
    M.jsonErr(404, errors.ErrorResponse$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
