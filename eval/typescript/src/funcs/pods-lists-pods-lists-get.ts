/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { AgentmailCliCore } from "../core.js";
import { encodeSimple } from "../lib/encodings.js";
import { matchStatusCode } from "../lib/http.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import * as components from "../models/components/index.js";
import { AgentmailCliError } from "../models/errors/agentmail-cli-error.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/http-client-errors.js";
import * as errors from "../models/errors/index.js";
import { ResponseValidationError } from "../models/errors/response-validation-error.js";
import { SDKValidationError } from "../models/errors/sdk-validation-error.js";
import * as operations from "../models/operations/index.js";
import { PodsListsGetServerList } from "../models/operations/pods-lists-get.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * Get List Entry
 *
 * @remarks
 * **CLI:**
 * ```bash
 * agentmail pods:lists get --pod-id <pod_id> --direction <direction> --type <type> --entry <entry>
 * ```
 */
export function podsListsPodsListsGet(
  client: AgentmailCliCore,
  podId: string,
  direction: components.Direction,
  type: components.ListType,
  entry: string,
  options?: RequestOptions,
): APIPromise<
  Result<
    components.PodListEntry,
    | errors.ErrorResponse
    | AgentmailCliError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    podId,
    direction,
    type,
    entry,
    options,
  ));
}

async function $do(
  client: AgentmailCliCore,
  podId: string,
  direction: components.Direction,
  type: components.ListType,
  entry: string,
  options?: RequestOptions,
): Promise<
  [
    Result<
      components.PodListEntry,
      | errors.ErrorResponse
      | AgentmailCliError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const input: operations.PodsListsGetRequest = {
    podId: podId,
    direction: direction,
    type: type,
    entry: entry,
  };

  const parsed = safeParse(
    input,
    (value) => z.parse(operations.PodsListsGetRequest$outboundSchema, value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;

  const baseURL = options?.serverURL
    || pathToFunc(PodsListsGetServerList[0], { charEncoding: "percent" })();

  const pathParams = {
    direction: encodeSimple("direction", payload.direction, {
      explode: false,
      charEncoding: "percent",
    }),
    entry: encodeSimple("entry", payload.entry, {
      explode: false,
      charEncoding: "percent",
    }),
    pod_id: encodeSimple("pod_id", payload.pod_id, {
      explode: false,
      charEncoding: "percent",
    }),
    type: encodeSimple("type", payload.type, {
      explode: false,
      charEncoding: "percent",
    }),
  };
  const path = pathToFunc("/v0/pods/{pod_id}/lists/{direction}/{type}/{entry}")(
    pathParams,
  );

  const headers = new Headers(compactMap({
    Accept: "application/json",
  }));

  const secConfig = await extractSecurity(client._options.bearerAuth);
  const securityInput = secConfig == null ? {} : { bearerAuth: secConfig };
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    options: client._options,
    baseURL: baseURL ?? "",
    operationID: "pods_lists_get",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: client._options.bearerAuth,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: baseURL,
    path: path,
    headers: headers,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    isErrorStatusCode: (statusCode: number) =>
      matchStatusCode({ status: statusCode } as Response, ["4XX", "5XX"]),
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result] = await M.match<
    components.PodListEntry,
    | errors.ErrorResponse
    | AgentmailCliError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(200, components.PodListEntry$inboundSchema),
    M.jsonErr(404, errors.ErrorResponse$inboundSchema),
    M.fail("4XX"),
    M.fail("5XX"),
  )(response, req, { extraFields: responseFields });
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
